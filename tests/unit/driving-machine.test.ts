import{describe,it,expect}from'vitest';import{DrivingMachine}from'../../src/driving/driving-machine';describe('condução',()=>{it('só abre depois de parar na faixa',()=>{const m=new DrivingMachine();m.close();m.accelerate();m.accelerate();m.accelerate();m.distance=80;m.brake();m.brake();m.brake();expect(m.state).toBe('stopped');expect(m.open()).toBe(true)});it('não soma distância em saltos ao acelerar',()=>{const m=new DrivingMachine();m.close();m.accelerate();expect(m.distance).toBe(0);expect(m.speed).toBe(20)})})
