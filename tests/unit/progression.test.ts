import{describe,it,expect}from'vitest';import{mastery}from'../../src/progression/mastery';describe('domínio',()=>{it('exige revisão e autonomia',()=>{const a=[{correct:true,usedHint:false,review:true},{correct:true,usedHint:false,review:false},{correct:true,usedHint:true,review:false},{correct:true,usedHint:false,review:false}].map((x,i)=>({...x,challengeId:String(i),at:''}));expect(mastery(a).mastered).toBe(true)})})
