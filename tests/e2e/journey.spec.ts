import{test,expect}from'@playwright/test';test('abre mapa acessível',async({page})=>{await page.goto('#/inicio');await page.getByLabel('Como você quer ser chamado?').fill('Felipe');await page.getByRole('button',{name:'Entrar no mapa'}).click();await expect(page.getByRole('heading',{name:/Escolha uma linha/})).toBeVisible();await expect(page.getByRole('button',{name:'Ver rota e embarcar'})).toHaveCount(9)})
