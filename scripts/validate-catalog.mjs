import fs from 'node:fs';const c=JSON.parse(fs.readFileSync(new URL('../public/data/catalog.v1.json',import.meta.url)));const errors=[];if(c.subjects.length!==9)errors.push('O catálogo deve ter 9 matérias');for(const s of c.subjects){if(s.stations.length!==3)errors.push(`${s.name}: requer 3 estações`);for(const st of s.stations){if(st.challenges.length!==2)errors.push(`${st.name}: requer 2 desafios`);for(const q of st.challenges)if(!q.approved||!q.prompt||!q.answer||!q.hint||!q.explanation)errors.push(`${q.id}: não aprovado ou incompleto`)}}if(errors.length){console.error(errors.join('\n'));process.exit(1)}console.log(`Catálogo ${c.version}: 9 linhas, 27 estações e 54 desafios aprovados.`)
