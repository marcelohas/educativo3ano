import type{AppData,Session}from'../domain/types';export function grantRewards(data:AppData,s:Session,stationId?:string){if(stationId){const k=`${s.id}:${stationId}`;if(!data.stamps.includes(k))data.stamps.push(k)}if(s.status==='completed'&&!data.tickets.includes(s.subjectId)){data.tickets.push(s.subjectId);data.trains=Math.floor(data.tickets.length/3)}}
