const KEY='estacoes-pin';async function digest(pin:string,salt:string){const bytes=new TextEncoder().encode(`${salt}:${pin}`);return Array.from(new Uint8Array(await crypto.subtle.digest('SHA-256',bytes))).map(x=>x.toString(16).padStart(2,'0')).join('')}export async function hasPin(){return!!localStorage.getItem(KEY)}export async function setPin(pin:string){if(!/^\d{4,8}$/.test(pin))throw new Error('Use de 4 a 8 números.');const salt=crypto.randomUUID();localStorage.setItem(KEY,JSON.stringify({salt,hash:await digest(pin,salt)}))}export async function verifyPin(pin:string){const saved=localStorage.getItem(KEY);if(!saved)return false;const{salt,hash}=JSON.parse(saved);return await digest(pin,salt)===hash}
