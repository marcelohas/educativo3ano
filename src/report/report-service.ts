import type{AppData}from'../domain/types';export function reportStats(data:AppData){const sessions=data.sessions.filter(s=>s.status==='completed');const attempts=sessions.flatMap(s=>s.attempts);return{sessions:sessions.length,attempts:attempts.length,correct:attempts.filter(a=>a.correct).length,withHelp:attempts.filter(a=>a.usedHint).length,review:attempts.filter(a=>a.review).length,stamps:data.stamps.length,tickets:data.tickets.length,trains:data.trains}}
