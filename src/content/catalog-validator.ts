import type{Catalog}from'../domain/types';export function validateCatalog(x:unknown):Catalog{const c=x as Catalog;if(!c||!Array.isArray(c.subjects)||c.subjects.length!==9)throw new Error('Catálogo inválido');const ids=new Set<string>();for(const s of c.subjects){if(s.stations.length!==3)throw new Error(`Rota incompleta: ${s.name}`);for(const st of s.stations){if(st.challenges.length!==2)throw new Error(`Estação incompleta: ${st.name}`);for(const q of st.challenges){if(ids.has(q.id)||!q.approved||!q.prompt||!q.answer||!q.hint||!q.explanation)throw new Error(`Desafio inválido: ${q.id}`);ids.add(q.id)}}}return c}
